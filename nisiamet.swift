/// Represents the configuration settings for the application.
struct AppConfig {
    /// The default timeout value for network requests, in seconds.
    static let defaultTimeout: TimeInterval = 30
}
